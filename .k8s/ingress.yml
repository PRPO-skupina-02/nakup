apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nakup
  namespace: production
  annotations:
    kubernetes.io/ingress.class: "gce"
spec:
  tls:
  - hosts:
    - nakup.cinecore.timhrovat.com
    secretName: nakup-tls
  rules:
  - host: nakup.cinecore.timhrovat.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: nakup
            port:
              number: 8080
